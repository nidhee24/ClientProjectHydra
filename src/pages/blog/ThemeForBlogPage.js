import { createMuiTheme } from '@material-ui/core/styles';

const ThemeForBlogPage = createMuiTheme({
    palette: {
        primary: {
            main: "#764abc"
        }
    },
    background: {
        default: "#fff"
    }
})

export default ThemeForBlogPage;